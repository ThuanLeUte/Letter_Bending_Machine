#ifndef __MAIN_H
#define __MAIN_H

enum StateMachine
{
  INIT_STATE = 0,
  RECIEVE_AND_RUNNING_STATE,
  CONFIG_STATE,
  FINISH_LETTER_STATE,
  FINISH_STATE,
  STOP_BUTTON_PRESS_STATE,
  EMERGENCY_STATE
};

enum execute
{
  EXECUTE_FIRST,
  EXECUTE_END
};

#endif __MAIN_H
